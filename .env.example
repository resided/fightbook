# FightBook Environment Variables
# Copy this file to .env and fill in your values
# For Vercel deployment: add these in Project Settings → Environment Variables

# Supabase (required for database persistence and API endpoints)
# Get these from your Supabase project: Settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# These are the same values — needed for Vercel serverless API routes
# which run in Node.js and can't access VITE_* vars at runtime
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
